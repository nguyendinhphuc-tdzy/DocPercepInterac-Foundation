diff --git a/node_modules/docx-preview/dist/docx-preview.mjs b/node_modules/docx-preview/dist/docx-preview.mjs
index 97fe1ba..ec110e7 100644
--- a/node_modules/docx-preview/dist/docx-preview.mjs
+++ b/node_modules/docx-preview/dist/docx-preview.mjs
@@ -2103,6 +2103,9 @@ class DocumentParser {
                 case "wrapNone":
                     wrapType = "wrapNone";
                     break;
+                case "docPr":
+                    result.foundationDrawingId = globalXmlParser.attr(n, "id");
+                    break;
                 case "graphic":
                     var g = this.parseGraphic(n);
                     if (g)
@@ -3392,6 +3395,18 @@ section.${c}>footer { z-index: 1; }
         }
     }
     renderElement(elem) {
+        const __node = this.renderElementInner(elem);
+        const __cb = this.options && this.options.onElementRendered;
+        if (__cb) {
+            if (Array.isArray(__node)) {
+                for (const __n of __node) if (__n && __n.nodeType === 1) __cb(elem, __n);
+            } else if (__node && __node.nodeType === 1) {
+                __cb(elem, __node);
+            }
+        }
+        return __node;
+    }
+    renderElementInner(elem) {
         switch (elem.type) {
             case DomType.Paragraph:
                 return this.renderParagraph(elem);
