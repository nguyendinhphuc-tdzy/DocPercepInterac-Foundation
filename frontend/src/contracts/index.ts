/**
 * Public export boundary for Foundation Contract v0.1 TypeScript projection.
 *
 * Applications and components should import contract types through this barrel:
 *   import type { FoundationTask, TaskStatus, ChangeProposal } from "./contracts";
 */

export type * from "./foundation";
