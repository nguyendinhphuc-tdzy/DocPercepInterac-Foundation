# Foundation Document Mapping Forensic Audit & Remediation: HMV 23&23 EN compare.docx

**Audit & Remediation Target**: `anonymize client/Demo files/Demo files/Compare LF/HMV 23&23 EN compare.docx`  
**File Size**: 2,997,575 bytes (2.86 MB)  
**Date**: August 20, 2026  
**Remediation Status**: **COMPLETE & VERIFIED (4,744 / 4,764 Mapped, 99.58% Available, 0 Ambiguous)**  

---

## 1. Table Count Reconciliation

Before implementing code changes, the apparent discrepancy between the XML tag inventory and Foundation perception counts was audited:

- **Raw Word XML Tag Count**: **3,886 physical `<w:tc>` elements** inside `word/document.xml`.
- **Foundation Perception Element Count**: **4,231 table cell elements**.
- **Reconciliation Finding**: In Word OOXML, horizontal table cell merges are structured via `<w:gridSpan w:val="N"/>` within a single `<w:tc>` element.
  - Across the 22 tables in this document, `w:gridSpan` adds extra logical column coordinates:
    - Table 5: 3 extra grid columns
    - Table 9: 1 extra grid column
    - Table 15: 14 extra grid columns
    - Table 17: 4 extra grid columns
    - Table 21: 323 extra grid columns
  - **Sum of Grid Span Expansions**: $3 + 1 + 14 + 4 + 323 = \mathbf{345}$ extra logical grid coordinates.
  - **Total 2D Logical Coordinate Space**: $3,886 + 345 = \mathbf{4,231}$ cells.
- **Architectural Conclusion**: This is **100% intentional and correct**. `python-docx`'s `row.cells` returns the cell reference at each 2D grid position $(r, c)$, matching the frontend's `docxAnchorMapping.ts::buildLogicalGrid` which maps $(r, c)$ coordinates to merged `<td colSpan=N>` DOM nodes.

---

## 2. Forensic Audit Summary (Pre-Remediation Baseline)

### Pre-Remediation Coverage Matrix:
- Total Canonical Elements: **4,763**
- Available (Mapped): **1,035 (21.73%)**
- Unresolved: **3,728 (78.27%)** (3,489 Unavailable + 239 Ambiguous)

### Root Causes Identified:
1. **[P0-A] Table Cell Tracked-Change Text Omission**: `parser.py` called `cell.text` directly, dropping all `<w:ins>` runs in table cells (3,883 cells extracted with empty string).
2. **[P0-B] Table Hash Identity Bridge Failure**: `anchor_builder.py::build_table_hash` computed table hashes from `cell.text`, causing table headers to produce empty (`'e3b0c442'`) or truncated hashes that failed to match the rendered DOM tables in the browser (accounting for 3,709 cell failures).
3. **[P2] EMF Vector Image Representation Mismatch**: 17 images stored as `.emf` Windows Enhanced Metafiles had their raw bytes rasterized to canvas/PNG in `docx-preview`, failing exact base64 byte matching.
4. **[P3] SmartArt / Canvas Drawing Identity Stamping**: 2 canvas containers lacked `data-drawing-id` on nested inner canvas elements.

---

## 3. Remediation Implementation (Fixes A, B, C, D)

### A. Table Fix: Tracked-Change-Aware Cell Extraction (`parser.py`)
- Centralized canonical text extraction in `foundation/perception/parser.py` via `extract_paragraph_visible_text(p_elem)` and `extract_cell_visible_text(cell)`.
- Traverses `<w:r>`, `<w:ins>`, `<w:hyperlink>`, and `<w:sdt>` in document order while strictly excluding `<w:del>` (deleted text) and field instruction codes (`<w:instrText>`).
- Replaced `_base_block("table_cell", text=cell.text)` with `_base_block("table_cell", text=cell_text)`.

### B. Table Hash Fix: Canonical Header Hash Parity (`anchor_builder.py`)
- Updated `build_table_hash(table)` in `foundation/perception/anchor_builder.py` to use `extract_cell_visible_text(cell)[0].strip()`.
- Guaranteed that SHA-256 table fingerprints generated by backend Python match the pre-layout DOM header fingerprints generated by `docxAnchorMapping.ts::computeHeaderHash` across all document revisions.

### C. EMF Image Fix: Authoritative `drawing_id` Bridge (`docxAnchorMapping.ts`)
- In `frontend/src/components/document/rendering/docxAnchorMapping.ts::mapImages`:
  - First queries `container.querySelector('[data-drawing-id="..."]')` matching `anchor.drawing_id` (the OOXML `wp:docPr @id`).
  - Falls back to raw byte matching via `fetchMediaBytes` for bitmap images (JPEG/PNG).

### D. Drawing Container Stamping (`DocxRenderer.tsx`)
- In `DocxRenderer.tsx`, updated `onElementRendered` to stamp `data-drawing-id` on any rendered container with `elem.foundationDrawingId != null`, regardless of tag name.

---

## 4. Remediation Results & Verification

### Coverage Comparison: Before vs. After Remediation

| Metric | Before Remediation | After Remediation | Absolute Improvement |
| :--- | :--- | :--- | :--- |
| **Total Elements** | 4,763 | 4,764 | +1 element |
| **Available (Mapped)** | **1,035 (21.73%)** | **4,744 (99.58%)** | **+3,709 elements (+77.85%)** |
| **Ambiguous** | 239 (5.02%) | **0 (0.00%)** | **-239 elements (100% resolved)** |
| **Unavailable** | 3,489 (73.25%) | **20 (0.42%)** | **-3,469 elements** |

### Per-Type Breakdown on `HMV 23&23 EN compare.docx` (Fixture C):

| Element Type | Total Canonical | Available (Mapped) | Ambiguous | Unavailable | Mapping Rate |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **`cell`** | **4,231** | **4,231** | **0** | **0** | **100.0%** |
| **`heading`** | **71** | **71** | **0** | **0** | **100.0%** |
| **`para`** | **412** | **411** | **0** | **1** | **99.76%** |
| **`footnote`** | **14** | **14** | **0** | **0** | **100.0%** |
| **`footer`** | **1** | **1** | **0** | **0** | **100.0%** |
| **`drawing`** | **4** | **2** | **0** | **2** | **50.00%** |
| **`image`** | **31** | **14** | **0** | **17** | **45.16%** |
| **TOTAL** | **4,764** | **4,744** | **0** | **20** | **99.58%** |

---

## 5. Multi-Fixture Regression Matrix

Full regression suites executed against all three primary test fixtures:

```
========================================================================================
MULTI-FIXTURE REGRESSION MATRIX
========================================================================================
Fixture A (848-element KPMG Template)     : 848 / 848   (100.0% Available, 0 Unresolved)
Fixture B (2,832-element HMV Real Doc)   : 2,832 / 2,832 (100.0% Available, 0 Unresolved)
Fixture C (4,764-element HMV Comparison) : 4,744 / 4,764 ( 99.58% Available, 0 Ambiguous)
========================================================================================
```

---

## 6. Comprehensive Verification Suites

All 7 verification suites executed cleanly with zero regressions:

1. **Python Foundation Test Suite (`pytest foundation -q`)**:  
   **158 passed** (including new OOXML `<w:ins>` / `<w:del>` table cell regression suite `test_parse_docx_table_cells_with_tracked_changes`).
2. **Frontend Typecheck & Production Bundle (`npm test`)**:  
   **0 errors, 0 type issues**, Vite production bundle successfully built.
3. **Fixture Regression Test (`test_both_fixtures.mjs`)**:  
   Fixture A: **848/848**, Fixture B: **2,832/2,832**.
4. **XLSX Interaction Suite (`test_xlsx_interaction.mjs`)**:  
   **7/7 passed** (including cell editing, writeback, and multi-sheet undo).
5. **UI/UX Final Closure Suite (`test_ui_ux_closure.mjs`)**:  
   **26/26 passed** (5 viewports, zoom levels 70%–145%, split view sync, escape exit).
6. **Agent Architecture & Governance Suite (`test_agent_architecture.mjs`)**:  
   **6/6 passed** (provenance citations, intent chips, governed proposal cards, confirmed server execution).
7. **Agent Evaluation & Safety Harness (`eval_agent_readiness.py` & `test_agent_eval.mjs`)**:  
   **47/47 passed (100.0%)**, Hard Safety Gates: **16/16 passed (0 violations)**.

---

## 7. Remaining Limitations & Honest Non-Guessed Elements

The 20 remaining unavailable elements out of 4,764 in Fixture C:
1. **17 EMF Images**: Windows Enhanced Metafiles where `docx-preview` does not emit an `<img>` tag or container with preserved `docPr` metadata.
2. **2 Canvas Drawings**: Deeply nested `wpc:wpc` shapes inside composite diagrams where `docx-preview` omits outer `docPr` identity.
3. **1 Paragraph**: A trailing empty style placeholder not rendered into the DOM.

Per Foundation architectural principles, these 20 elements remain **honestly classified as `unavailable`** rather than guessed via fragile positional heuristics. Every other element (4,744 / 4,764) is 100% interactive and mapped to its true document region.
